fastapi>=0.100.0
uvicorn>=0.23.0
pillow>=10.0.0
numpy>=1.24.0
python-multipart>=0.0.6
pydantic>=1.10.0,<2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
psutil>=5.9.0

# Note: PyTorch, transformers, accelerate, bitsandbytes, sentencepiece
# are not available as pre-built wheels for RISC-V architecture.
# Using basic uvicorn (without [standard] extras) to avoid Rust-dependent packages.
# Using Pydantic v1.x to avoid Rust compilation issues on RISC-V.
# Service will use Hugging Face Inference API instead of local model.
